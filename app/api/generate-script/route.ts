import { NextResponse } from "next/server";

export async function POST() {
  // TODO: Claude API integration for script generation
  return NextResponse.json({ message: "Generate script endpoint" });
}
